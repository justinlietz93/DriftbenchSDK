# """Kubernetes ConfigMap for DriftBench SDK."""

apiVersion: v1
kind: ConfigMap
metadata:
  name: driftbench-config
data:
  # TODO: Add non-sensitive configuration key-value pairs here
  # Example:
  # database.url: "sqlite:////app/driftbench_log.db" # Adjust path if needed inside container
  # log.level: "INFO"

  # Note: Sensitive data like API keys or passwords should use Kubernetes Secrets, not ConfigMaps.
